/**
 * Static media is bundled into `dist/` / Capacitor www (schools, images, videos).
 * This script is intentionally a no-op so production builds keep those assets.
 */

console.log('[strip-bundled-cdn-media] skipped — static media is bundled in the frontend (no S3 dependency).');
